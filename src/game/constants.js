/**
 * @const {number[]} LANES - Identifiants numériques pour les voies du jeu (haut, milieu, bas).
 */
export const LANES = [0, 1, 2];       

/**
 * @const {number} TRACK_SPEED - Vitesse de défilement initiale du décor.
 */
export const TRACK_SPEED = 4;

/**
 * @const {number} RUNNER_FRAME_SPEED - Facteur de vitesse pour l'animation du coureur.
 */
export const RUNNER_FRAME_SPEED = 0.15;

/**
 * @const {number} NUM_OPTIONS - Le nombre total d'options de réponse à afficher pour chaque question.
 */
export const NUM_OPTIONS = 3;

/**
 * @const {string} TRIVIA_URL - L'URL de base pour l'API de questions The Trivia API.
 */
export const TRIVIA_URL = "https://the-trivia-api.com/api/questions";
